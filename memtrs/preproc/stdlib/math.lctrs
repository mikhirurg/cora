/* =================================== MATH FUNCTIONS =========================================== */

min :: Int -> Int -> Int
min(x, y) -> x | x <= y
min(x, y) -> y | y < x

max :: Int -> Int -> Int
max(x, y) -> x | x >= y
max(x, y) -> y | y > x

/* ============================================================================================== */